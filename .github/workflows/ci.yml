# name: CI

# on:
#   push:
#     branches: [main]
#   pull_request:
#     branches: [main]

# jobs:
#   verify:
#     runs-on: ubuntu-latest

#     services:
#       postgres:
#         image: postgres:16-alpine
#         env:
#           POSTGRES_USER: boilerplate
#           POSTGRES_PASSWORD: boilerplate_dev
#           POSTGRES_DB: boilerplate_test
#         ports: ['5432:5432']
#         options: >-
#           --health-cmd "pg_isready -U boilerplate"
#           --health-interval 10s --health-timeout 5s --health-retries 5

#     env:
#       DATABASE_URL: postgresql://boilerplate:boilerplate_dev@localhost:5432/boilerplate_test?schema=public
#       JWT_SECRET: ci-secret-ci-secret-ci-secret-0123456789
#       RUN_INTEGRATION: '1'

#     steps:
#       - uses: actions/checkout@v4

#       - uses: actions/setup-node@v4
#         with:
#           node-version: 22
#           cache: npm

#       - name: Install
#         run: npm ci

#       - name: Generate Prisma client
#         run: npx prisma generate

#       - name: Lint
#         run: npm run lint

#       - name: Typecheck
#         run: npm run typecheck

#       - name: Migrate + seed test DB
#         run: |
#           npx prisma migrate deploy
#           npm run prisma:seed

#       - name: Test (unit + integration)
#         run: npm test

#       - name: Build
#         run: npm run build

#       - name: Docker build
#         run: docker build -f docker/Dockerfile -t nodejs-boilerplate:${{ github.sha }} .
